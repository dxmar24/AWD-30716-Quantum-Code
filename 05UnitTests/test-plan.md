# Test Plan
- Unit tests: attendance rate, scholarship threshold, teacher payment.
- Integration tests: REST endpoints with role authorization.
- Auth/session tests: login, logout, revoked/expired sessions, invalid role, no-store private pages.
