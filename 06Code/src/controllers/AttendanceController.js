const { ApiResponse } = require('../utils/ApiResponse');
class AttendanceController { constructor(attendanceService) { this.attendanceService = attendanceService; } recordStudent = (req, res, next) => { try { return ApiResponse.success(res, this.attendanceService.recordStudentAttendance(req.sessionUser, req.body), 'Attendance recorded', 201); } catch (e) { return next(e); } }; teacherCheckIn = (req, res, next) => { try { return ApiResponse.success(res, this.attendanceService.checkInTeacher(req.sessionUser, req.body), 'Teacher checked in', 201); } catch (e) { return next(e); } }; teacherCheckOut = (req, res, next) => { try { return ApiResponse.success(res, this.attendanceService.checkOutTeacher(req.sessionUser, req.params.id), 'Teacher checked out'); } catch (e) { return next(e); } }; }
module.exports = { AttendanceController };
