const { ApiResponse } = require('../utils/ApiResponse');
class AttendanceController { constructor(attendanceService) { this.attendanceService = attendanceService; } recordStudent = async (req, res, next) => { try { return ApiResponse.success(res, await this.attendanceService.recordStudentAttendance(req.sessionUser, req.body), 'Attendance recorded', 201); } catch (e) { return next(e); } }; teacherCheckIn = async (req, res, next) => { try { return ApiResponse.success(res, await this.attendanceService.checkInTeacher(req.sessionUser, req.body), 'Teacher checked in', 201); } catch (e) { return next(e); } }; teacherCheckOut = async (req, res, next) => { try { return ApiResponse.success(res, await this.attendanceService.checkOutTeacher(req.sessionUser, req.params.id), 'Teacher checked out'); } catch (e) { return next(e); } }; }
module.exports = { AttendanceController };
