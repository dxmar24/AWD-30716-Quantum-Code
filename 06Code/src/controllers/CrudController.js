const { ApiResponse } = require('../utils/ApiResponse');
class CrudController { constructor(repository, entityName) { this.repository = repository; this.entityName = entityName; } index = async (req, res) => ApiResponse.success(res, await this.repository.all(), `${this.entityName} list`); show = async (req, res) => { const row = await this.repository.findById(req.params.id); return row ? ApiResponse.success(res, row) : ApiResponse.error(res, `${this.entityName} not found`, 404); }; create = async (req, res) => ApiResponse.success(res, await this.repository.create(req.body), `${this.entityName} created`, 201); update = async (req, res) => { const row = await this.repository.update(req.params.id, req.body); return row ? ApiResponse.success(res, row, `${this.entityName} updated`) : ApiResponse.error(res, `${this.entityName} not found`, 404); }; }
module.exports = { CrudController };
