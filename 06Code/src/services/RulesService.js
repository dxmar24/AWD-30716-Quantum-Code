class RulesService { constructor(db, attendanceService) { this.db = db; this.attendanceService = attendanceService; } scholarshipCandidate(studentId, from, to) { const attendanceRate = this.attendanceService.attendanceRate(studentId, from, to); return { studentId, attendanceRate, candidate: attendanceRate >= 0.9, rule:'>= 90% attendance in two-month period; director evaluation required' }; } promotionCandidate(studentId, from, to) { const attendanceRate = this.attendanceService.attendanceRate(studentId, from, to); return { studentId, attendanceRate, candidate: attendanceRate >= 0.85, rule:'Attendance and consistency suggest B1 to B2 evaluation; theory/practice approval required' }; } teacherPayment(teacherId) { const rows = this.db.teacherAttendance.filter((r) => r.teacherId === teacherId && r.checkInAt && r.checkOutAt); const hours = rows.reduce((sum, r) => sum + ((new Date(r.checkOutAt) - new Date(r.checkInAt)) / 3600000), 0); const teacher = this.db.teachers.findById(teacherId); const hourlyRate = teacher?.hourlyRate || 12.5; return { teacherId, hours: Number(hours.toFixed(2)), hourlyRate, amount: Number((hours * hourlyRate).toFixed(2)) }; } }
module.exports = { RulesService };
