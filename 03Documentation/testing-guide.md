# Testing Guide
Run tests from `06Code` using `npm test`. Test focus: login, logout, expired/revoked session, invalid role, private no-store behavior, attendance rate, scholarship threshold and teacher payment calculations.
